<h1>Known_link creation</h1>

<form action="{{ path('known_link_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('known_link') }}">
            Back to the list
        </a>
    </li>
</ul>
