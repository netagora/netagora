<h1>Twitter list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>User</th>
            <th>Token</th>
            <th>Twitter_id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('twitter_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.user }}</td>
            <td>{{ entity.token }}</td>
            <td>{{ entity.twitterid }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('twitter_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('twitter_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('twitter_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
